# פקעות — אפליקציית מעקב פקודות עבודה

כלי אישי לצבעי במפעל מרססי דגניה. מעקב אחרי פקודות עבודה (פק״ע) ומק״טים,
ודיווח כמויות שנצבעו.

השפה של הממשק היא **עברית, RTL**. כל טקסט חדש בממשק צריך להיות בעברית.

## מצב נוכחי

קובץ יחיד: `index.html` — HTML + CSS + JS, ללא build step וללא dependencies
חוץ מ-Google Fonts.

### פיצ׳רים קיימים

- שני טאבים: **פקעות** (עם מספר פק״ע + הזמנה) ו**מק״טים** (ללא פק״ע)
- עריכה של פריט בלחיצה על הכרטיס; הוספה ידנית יושבת בתפריט הצדדי
- **סריקה מצילום**: מצלמים דף פק״ע, והשדות מתמלאים אוטומטית
  (מספר פק״ע, תיאור, מק״ט, כמות, תאריך, מספר הזמנה + שורה)
- בטאב מק״טים הסריקה מזהה כמה מק״טים בדף ומציגה רשימה לבחירה
- כפתור **נצבעו**: בוחרים פריטים, רושמים כמות לכל אחד, ואז שואל כמות עגלות
  (אפשר לדלג) ונוצר טקסט סיכום מקובץ לפי סוג (פקעות קודם, מק״טים אחר כך)
  עם כפתור העתקה. הכמות יורדת מהמלאי והפריט נסגר כשנגמר.
- חיפוש חופשי. אין כפתורי סינון — הרשימה הראשית מציגה תמיד רק
  את מה שעדיין במלאי.
- **תפריט צדדי** (☰ בכותרת): הוספה ידנית, הושלמו, ייצוא לאקסל, גיבוי להעברה
  למכשיר אחר, ייבוא גיבוי
- **הושלמו**: חלון בתפריט עם כל הפריטים שנסגרו, מקובץ לפקעות ומק״טים
- **ייצוא לאקסל**: קובץ `.xlsx` אמיתי עם 6 גיליונות — סיכום, פקעות,
  מק״טים, דיווחי צביעה, עגלות, עגלות לפי חודש
- **היסטוריית דיווחים**: כפתור דו״חות בכותרת פותח:
  - **דו״ח עגלות חודשי** — סה״כ עגלות לכל חודש, עם כפתור העתקה לכל חודש
  - **פירוט צביעה** — רשימת כל מה שדווח כנצבע, מקובץ לפי תאריך, עם שעה,
    כמות ותיאור לכל דיווח

### מבנה הנתונים

מערך אחד ב-storage תחת המפתח `work-orders-list`:

```js
{
  id, kind: 'pka' | 'sku',
  pka, sku, desc,
  qty, unit, date,
  orderNumber, orderRow,
  done, paintedQty, v
}
```

מערך נוסף תחת `work-orders-history` — שורה אחת לכל פריט שדווח כנצבע:

```js
{ id, ts /* ISO timestamp */, kind, label, desc, qty, unit }
```

ומערך שלישי תחת `work-orders-carts` — שורה אחת לכל סבב "נצבעו" שבו הוזנה
כמות עגלות (>0). דו״ח העגלות החודשי בהיסטוריה מסכם לפי חודש (`monthKey`):

```js
{ id, ts /* ISO timestamp */, count }
```

### גיבוי / שחזור בין מכשירים

כפתור "גיבוי להעברה למכשיר אחר" בתפריט בונה קישור שמכיל את כל הנתונים
(`items`, `history`, `carts`) כ-JSON מקודד ב-base64 בתוך ה-hash של ה-URL
(`#restore=...`) — לא נשלח לשום שרת. משתמש ב-`navigator.share` אם קיים
(לשלוח דרך הודעות/מייל/AirDrop), אחרת מעתיק את הקישור ללוח. פתיחת הקישור
טוענת את `checkRestoreHash()` שמציגה `confirm()` עם כמות הפריטים החדשים
(מסונן לפי `id` כדי לא לשכפל) ומוסיפה אותם לנתונים הקיימים — לא מוחקת כלום.

**חשוב לדעת:** אפליקציה שהותקנה למסך הבית באייפון (`Add to Home Screen`)
מקבלת אחסון `localStorage` **נפרד** מ-Safari הרגיל לאותו אתר — וזה עובד
גם בכיוון ההפוך: קישור `#restore=` שנפתח ונאושר בתוך Safari **לא** מגיע
לאפליקציה המותקנת, כי הן שתי תיבות אחסון נפרדות (לא רק בזמן מחיקה/התקנה
מחדש). מחיקת הקיצור ממסך הבית מוחקת את האחסון הזה לצמיתות — זה מה שקרה
בפועל וגרם לאובדן הנתונים שהוביל להוספת הפיצ׳ר הזה. לפני שמנחים למחוק
ולהוסיף קיצור מחדש (למשל כדי לרענן אייקון), **קודם ליצור גיבוי דרך הכפתור
הזה.**

בגלל הבידוד הזה, "ייבוא גיבוי" (הדבקת הקוד/קישור בתוך תיבת טקסט) הוא
הדרך האמינה להכניס גיבוי **לתוך האפליקציה המותקנת עצמה** — צריך לפתוח
אותו מהאייקון במסך הבית ולהדביק שם, לא לסמוך על קישור שנפתח ב-Safari.

### ייצוא לאקסל

`xlsxBytes()` בונה קובץ `.xlsx` אמיתי בלי שום תלות חיצונית: חלקי החוברת הם
XML רגיל, ארוזים ב-ZIP עם entries לא דחוסים (`zipStore`, method 0 + CRC32).
אם צריך להוסיף גיליון — מוסיפים אובייקט `{name, rows, widths, frozen}`
ב-`buildReportSheets()`; תא נבנה עם `cTxt` / `cNum` / `cDate` / `cHead`.
תאריכים נשמרים כמספר סידורי של אקסל עם פורמט `dd/mm/yyyy`, וכל גיליון
מוגדר `rightToLeft`.

## חסמים לפני שזה יכול לרוץ מחוץ ל-Claude Artifacts

הקוד נכתב בתוך Claude Artifacts ומסתמך על שני דברים שלא קיימים בדפדפן רגיל.

### 1. ~~`window.storage` לא קיים מחוץ ל-Artifacts~~ ✅ פתור

`loadData()` ו-`saveData()` עברו ל-`localStorage`.

### 2. ~~קריאת ה-API לסריקה עובדת בלי מפתח רק בתוך Artifacts~~ ✅ פתור

`callClaude()` קוראת ל-proxy ב-`worker/` (Cloudflare Worker, `pkaot-scan-proxy`)
במקום ישירות ל-Anthropic. ה-worker מחזיק את מפתח ה-API כ-secret
(`ANTHROPIC_API_KEY`, הוגדר עם `wrangler secret put`) ומעביר את הבקשה הלאה.

- כתובת ה-proxy: `https://pkaot-scan-proxy.quixonn.workers.dev`
- ה-worker מקבל בקשות רק מ-Origin של `https://quixonn-alt.github.io` (מוגדר
  ב-`ALLOWED_ORIGIN` ב-`worker/src/index.js`) — לשנות שם אם ה-hosting זז.
- פריסה: `cd worker && npx wrangler deploy` (דורש `wrangler login` פעם אחת).
- אם צריך להחליף/לחדש את המפתח: `cd worker && npx wrangler secret put ANTHROPIC_API_KEY`.

עדיין יש נפילה להוספה ידנית אם הסריקה נכשלת (`showScanError`).

## יעד

PWA שמותקנת במסך הבית של הטלפון:

- ✅ `manifest.json` + service worker (`sw.js`) לעבודה offline
- ✅ אייקון (`icon-192.png`, `icon-512.png`, `apple-touch-icon.png`)
- ✅ hosting ב-HTTPS (GitHub Pages) — חובה בשביל גישה למצלמה

## שפת העיצוב — תעשייתי אלגנטי (ירוק/צהוב דגניה)

עוצב מחדש מברוטליזם תעשייתי (כתום/שחור/בטון) לפלטת הצבעים של מרססי
דגניה — ירוק וצהוב — בגרסה מאופקת, לא צעקנית.

- **צבעים:** ירוק-שחור עמוק `#122019` (טקסט, מסגרות, כותרת) על בטון קריר
  `#EEF0E9`, משטחים לבנים. ירוק דגניה `#1E8E52` = פעולה ראשית / פריט פתוח /
  בחירה (במקום הכתום הישן). צהוב מאופק `#E3BE4A` = אזהרה וחוסר מידע (פחות
  זעקני מהצהוב התעשייתי הקודם). ירוק בהיר `#35C97D` = אישור/סיום — מובחן
  בבירור מהירוק הראשי כדי שלא יתבלבלו. תמיד טקסט כהה (`--ink`) על צבע, לא לבן.
- **מסגרות 3–4px וצל קשיח** (`-5px 5px 0`) נשארו — משוב מישושי לשימוש
  בעמידה עם כפפות, ראה עקרונות תכנון למטה. **פינות מעוגלות קלות (4–14px,
  משתנה לפי רכיב)** הוחלפו במקום זווית חדה לגמרי, לתחושה עתידנית ופחות
  "תעשייתי גס". לחיצה עדיין מזיזה את האלמנט לתוך הצל
  (`translate(-5px,5px)` + ביטול הצל).
- **היררכיה בהיפוך ולא בגוון:** מצב פעיל = מילוי כהה וטקסט לבן. כפתור
  מושבת = פסי הצללה אלכסוניים, לא אפור דהוי.
- **פס מפריד** בין הכותרת לתוכן (`.hazard`) הוא כעת קו דק (4px) במעבר
  גרדיאנט ירוק→צהוב, במקום פסי אזהרה שחור/צהוב — פחות "סרט הזהרה", יותר
  קו מותג.
- **טיפוגרפיה:** Heebo 900 לכותרות וכפתורים, Rubik לגוף, JetBrains Mono
  למספרים ומזהים. מספרים תמיד גדולים — הם המידע הקריטי.

אם משנים צבע יסוד נוסף — לעדכן את משתני ה-CSS ב-`:root` בלבד; כמעט כל
הרכיבים צורכים אותם דרך `var(--ink|--paper|--hi|--yellow|--go|--muted)`
ולא צובעים ידנית.

## עקרונות תכנון

- **מובייל בלבד.** נעשה בו שימוש בעמידה, ביד אחת, עם כפפות עבודה.
  אזורי לחיצה: 60px לפעולות ראשיות (נצבעו, צילום — יושבים בדיוק על הרף),
  64px לכפתורי ± וקלט כמות, 30px לתיבות סימון. אל תקטין אותם.
  אם יבקשו להקטין עוד — לעדכן את הכלל כאן במפורש, לא לחרוג ממנו בשקט.
- **מסך מלא באייפון.** האפליקציה רצה עם `viewport-fit=cover` וסטטוס־בר
  שקוף, כלומר התוכן מתחיל ב-y=0 מתחת לשעון ולסוללה. כל אלמנט שנוגע
  בקצה העליון חייב `env(safe-area-inset-top)`: הכותרת בריפוד, פס
  החיפוש הדביק ב-`top`, ו-`.status-guard` צובע שחור מאחורי הסטטוס־בר
  לרגעים שהכותרת נגללה. אל תסיר אותם.
- **הרשימה הראשית = מה שבמלאי.** מה שהושלם עובר לחלון בתפריט, לא למסנן
  על הרשימה. מסנן שנשאר דלוק בין הפעלות נראה בדיוק כמו נתונים שנמחקו,
  וזו חרדה אמיתית של המשתמש. חלון שסוגרים לא יכול לעשות את זה.
- **פקעות ממוינות לפי המספר, מהגדול לקטן.** מספר גבוה = חדש. פק״ע ישנה
  שתקועה שבועות יורדת לתחתית במקום לחסום את ראש הרשימה. פק״ע בלי ספרות
  יורדת לסוף. מק״טים אין להם מספר כזה ונשארים לפי תאריך.
- **± בצד ימין.** בכל שדה כמות (צביעה, עגלות) כפתור `+` הוא הימני —
  הצד הנגיש לאגודל ביד ימין, וזו הפעולה הנפוצה.
- **רטט קצר** (`tick()`) על בחירה, על ± ועל אישור סיום — אישור פיזי
  מהיר יותר מאישור ויזואלי כשהידיים עסוקות.
- **כרטיס קומפקטי.** שתי שורות: מזהה + כמות + מספר הזמנה (ספרות הסוף בלבד),
  ומתחת התיאור. פס כתום בקצה = פתוח. פרטים מלאים נפתחים בלחיצה על הכרטיס.
  הכרטיס עבר כיווץ (ריפוד 9px, תיאור 0.88rem) — עוד פריטים על המסך.
- **בלי מדדי לחץ.** אין מונה פקעות פתוחות ואין אחוזי התקדמות. הכלי מדווח
  מה נעשה, הוא לא מתמרץ ולא מודד קצב. אל תוסיף כאלה.
- **כמויות לעולם לא מגיעות ל-100.** בדפי המפעל הכמות מודפסת כ-`N.NNN`
  והנקודה היא עשרונית: `3.000` = שלוש. `normalizeQty()` מחלק ב-1000 כל ערך
  שיוצא 100 ומעלה. אל תסיר את הבדיקה הזו.
- **הסריקה מציעה, המשתמש מאשר.** תמיד להראות את מה שנקרא לפני שמירה.

## פריסה

`index.html` ו-`sw.js` יושבים ב-GitHub Pages
(`https://quixonn-alt.github.io/work/`) ונדחפים ב-push ל-`master`.
**בכל שינוי ב-`index.html` חייבים להעלות את `CACHE_NAME` ב-`sw.js`** —
אחרת ה-service worker ימשיך להגיש את הגרסה השמורה. הנתונים יושבים
ב-`localStorage` של הדפדפן בטלפון, לא ב-git, ופריסה לא נוגעת בהם.

## רעיונות להמשך

- סריקת ברקוד במקום צילום הדף (יש ברקוד על כרטיס העבודה) — לבדוק קודם
  מה בדיוק מקודד בברקוד (כנראה רק מספר פק״ע, לא כמות/תאריך/הזמנה)
- קיבוץ פקעות לפי מספר הזמנה
