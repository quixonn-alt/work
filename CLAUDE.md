# פקעות — אפליקציית מעקב פקודות עבודה

כלי אישי לצבעי במפעל מרססי דגניה. מעקב אחרי פקודות עבודה (פק״ע) ומק״טים,
ודיווח כמויות שנצבעו.

השפה של הממשק היא **עברית, RTL**. כל טקסט חדש בממשק צריך להיות בעברית.

## מצב נוכחי

קובץ יחיד: `index.html` — HTML + CSS + JS, ללא build step וללא dependencies
חוץ מ-Google Fonts.

### פיצ׳רים קיימים

- שני טאבים: **פקעות** (עם מספר פק״ע + הזמנה) ו**מק״טים** (ללא פק״ע)
- הוספה ידנית ועריכה של פריט
- **סריקה מצילום**: מצלמים דף פק״ע, והשדות מתמלאים אוטומטית
  (מספר פק״ע, תיאור, מק״ט, כמות, תאריך, מספר הזמנה + שורה)
- בטאב מק״טים הסריקה מזהה כמה מק״טים בדף ומציגה רשימה לבחירה
- כפתור **נצבעו**: בוחרים פריטים, רושמים כמות לכל אחד, נוצר טקסט סיכום
  עם כפתור העתקה. הכמות יורדת מהמלאי והפריט נסגר כשנגמר.
- חיפוש + סינון (הכול / פתוחים / הושלמו)

### מבנה הנתונים

מערך אחד ב-storage תחת המפתח `work-orders-list`:

```js
{
  id, kind: 'pka' | 'sku',
  pka, sku, desc,
  qty, unit, date,
  orderNumber, orderRow,
  done, paintedQty, v
}
```

## שני חסמים לפני שזה יכול לרוץ מחוץ ל-Claude Artifacts

הקוד נכתב בתוך Claude Artifacts ומסתמך על שני דברים שלא קיימים בדפדפן רגיל.
**אלה שתי המשימות הראשונות.**

### 1. `window.storage` לא קיים מחוץ ל-Artifacts

הפונקציות `loadData()` ו-`saveData()` קוראות ל-`window.storage.get/set`.
צריך להחליף ב-`localStorage` (או IndexedDB אם רוצים גיבוי/ייצוא בהמשך).
ה-API הפנימי כבר async, אז ההחלפה נקייה.

### 2. קריאת ה-API לסריקה עובדת בלי מפתח רק בתוך Artifacts

`callClaude()` עושה `fetch` ל-`https://api.anthropic.com/v1/messages` בלי
API key — זה עובד רק בסביבת Artifacts.

**אסור להטמיע API key בקוד צד-לקוח.** הפתרון: proxy קטן
(Cloudflare Workers / Vercel Function) שמחזיק את המפתח ומעביר את הבקשה.
ה-frontend קורא ל-proxy במקום ישירות ל-Anthropic.

עד שזה מוכן — הסריקה תיכשל אבל האפליקציה תמשיך לעבוד, כי יש נפילה
להוספה ידנית (`showScanError`).

## יעד

PWA שמותקנת במסך הבית של הטלפון:

- `manifest.json` + service worker לעבודה offline
- אייקון
- hosting ב-HTTPS (GitHub Pages מספיק) — חובה בשביל גישה למצלמה

## עקרונות תכנון

- **מובייל בלבד.** נעשה בו שימוש בעמידה, ביד אחת, עם כפפות עבודה.
  אזורי לחיצה גדולים, מעט גלילה.
- **כרטיס קומפקטי.** שתי שורות: מזהה + כמות + מספר הזמנה (ספרות הסוף בלבד,
  בירוק), ומתחת התיאור. פרטים מלאים נפתחים בלחיצה על הכרטיס.
- **כמויות לעולם לא מגיעות ל-100.** בדפי המפעל הכמות מודפסת כ-`N.NNN`
  והנקודה היא עשרונית: `3.000` = שלוש. `normalizeQty()` מחלק ב-1000 כל ערך
  שיוצא 100 ומעלה. אל תסיר את הבדיקה הזו.
- **הסריקה מציעה, המשתמש מאשר.** תמיד להראות את מה שנקרא לפני שמירה.

## רעיונות להמשך

- ייצוא הסיכומים / כל הרשימה לאקסל או CSV
- היסטוריית דיווחי צביעה (מה נצבע ומתי)
- סריקת ברקוד במקום צילום הדף (יש ברקוד על כרטיס העבודה)
- גיבוי וסנכרון בין מכשירים
- קיבוץ פקעות לפי מספר הזמנה
